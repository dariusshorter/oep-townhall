{
  "name": "oep-town-hall-anonymous-link",
  "version": "1.0.0",
  "private": true,
  "description": "Anonymous town hall question portal for Origin Exterior Partners.",
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build && npm --workspace api run build",
    "test": "npm --workspace frontend run test && npm --workspace api run test",
    "lint": "npm --workspace frontend run lint && npm --workspace api run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
